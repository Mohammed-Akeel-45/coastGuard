import { Router } from "express";
const router = Router();
router.post("/login");
router.post("/regitser");
router.post("/revoke");
router.post("/refresh");
export default router;
//# sourceMappingURL=auth.routes.js.map